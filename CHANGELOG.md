# Changelog

## Unreleased

- Added project-local agent context in `PROJECT.md`.
- Added project-local ignore rules in `.gitignore`.
- Added `.env.example` for optional provider API keys.
- Added `TASKS.md` to track next actions and backlog.
